// Create an array and populate
var colors = ["red", "orange", "yellow", "green"]

// Iterate through the array and print each item to the console
for(var i = 0; i < colors.length; i++){
	console.log(colors[i]);
}